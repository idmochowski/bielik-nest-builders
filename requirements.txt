# NeMo Agent Toolkit (core + langchain + crewai)
nvidia-nat==1.6.0
nvidia-nat-langchain==1.6.0
nvidia-nat-crewai==1.6.0

# Observability
nvidia-nat-phoenix==1.6.0
arize-phoenix>=14,<15

# Utilities
python-dotenv==1.2.2
